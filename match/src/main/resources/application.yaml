spring:
  application:
    name: "match"
  datasource:
    url: "jdbc:mysql://localhost:3306/cloud6?useSSL=false&serverTimezone=UTC"
    username: "root"
    password: "12345678"
  jpa:
    hibernate:
      ddl-auto: "create-drop"
